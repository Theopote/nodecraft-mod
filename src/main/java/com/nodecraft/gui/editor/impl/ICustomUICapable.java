package com.nodecraft.gui.editor.impl;

/**
 * 标记接口：表示节点可能具有自定义UI功能
 * 
 * 实现此接口的节点类表明它们可能通过反射方式提供自定义UI功能。
 * 这个接口用于优化性能，避免对明显不具有自定义UI功能的节点进行反射检查。
 * 
 * 使用场景：
 * 1. 对于实现了 ICustomUINode 接口的节点，无需实现此接口（直接使用接口方法）
 * 2. 对于通过反射提供自定义UI的节点，应实现此接口以避免被跳过
 * 3. 对于确定不具有自定义UI的节点，不实现此接口可提升性能
 * 
 * 性能优化：
 * - 减少不必要的反射调用
 * - 提供明确的自定义UI能力标识
 * - 支持渐进式迁移到 ICustomUINode 接口
 */
public interface ICustomUICapable {
    // 标记接口，无需实现任何方法
    // 仅用于类型检查和性能优化
} 